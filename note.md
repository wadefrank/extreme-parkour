# Extreme Parkour with Legged Robots — 项目详解

这是一个**让四足机器人学会跑酷的强化学习框架**，来自 CMU 的研究团队（Xuxin Cheng, Kexin Shi 等人，2023 年发表于 arXiv）。

## 核心目标

训练四足机器人（A1、Go1、XTDog）在复杂地形上执行高难度跑酷动作：**跳跃障碍、跨越缝隙、攀爬台阶、在不规则地形上快速奔跑**——这些动作对机器人的感知和运动控制都是极大的挑战。

## 两阶段训练流程

这是该项目最核心的设计：

### 第一阶段：Base Policy（基础策略训练）
- 使用 NVIDIA Isaac Gym 进行 GPU 并行仿真（默认 6144 个环境同时训练）
- 机器人**只依赖本体感知**（关节角度、角速度、IMU 等 53 维观测）+ 地形高度扫描（132 维 scandots）
- 训练时有"特权信息"（privileged info）：真实的线速度、摩擦系数、质量参数等——这些信息在真实部署时是拿不到的
- 通过 PPO 算法训练 10-15k 轮（约 8-10 小时），让机器人学会基本运动技能
- 同时训练一个 **Estimator**（估计器）来从本体感知预测特权信息，以及一个 **History Encoder**（历史编码器）通过 DAgger 蒸馏学习用历史观测替代特权信息

### 第二阶段：Distillation（视觉蒸馏训练）
- 加入深度相机（58×87 分辨率），让机器人"看见"前方地形
- **视觉编码器**（CNN + GRU）学习将深度图压缩为 32 维潜在向量 + 2 维偏航修正
- 目标是让视觉策略的输出**模仿第一阶段训练好的 base policy 的动作**
- 这就是"蒸馏"：teacher（base policy 有 scandots 特权信息）→ student（只有深度相机）

## 地形课程学习（Terrain Curriculum）

仿真中有一个 10×40 的地形网格：
- **行**代表难度（0-9 级递增）
- **列**代表不同地形类型：平坡、粗糙坡、台阶、跑酷障碍、缝隙、跳台等 20+ 种
- 机器人表现好就升级到更难的地形，表现差就降级——**自适应难度调节**

## Domain Randomization（域随机化）

为了让策略能从仿真迁移到真实机器人（sim-to-real），训练时随机化：
- 地面摩擦系数 [0.6, 2.0]
- 机体附加质量 [0, 3kg] 和质心偏移
- 电机力矩强度 [0.8×, 1.2×]
- 动作延迟（模拟真实通信延迟）

## 部署流程

训练完成后通过 `save_jit.py` 导出 JIT 模型，可以直接部署到真实的四足机器人上。机器人只需要：
- 12 个关节的编码器（角度、角速度）
- IMU（横滚、俯仰）
- 一个前向深度相机

## 总结

简单来说：**在 GPU 仿真里用强化学习大规模训练 → 用课程学习逐步提高难度 → 用视觉蒸馏让机器人学会"看路" → 通过域随机化确保能迁移到现实世界**。最终让真实的四足机器人能够像动物一样在复杂地形上跑酷。

---

# XTDog PD 控制参数调参指南

## 核心公式

```
τ = Kp × (q_target - q) - Kd × q̇
q_target = action_scale × action + default_angle
action ∈ [-1.2, 1.2]  (clip_actions)
```

所以**最大位置力矩**：

```
τ_max = Kp × action_scale × clip_actions = Kp × action_scale × 1.2
```

这个值必须满足两个条件：
- **不超过** URDF 力矩上限（40 N·m）
- **足够大**，能支撑 28kg 机体做跑酷动作（跳跃、攀爬）

## 各参数的作用

| 参数 | 调大 | 调小 |
|------|------|------|
| **stiffness (Kp)** | 关节更硬、力矩更大，但可能振荡 | 关节更软、顺从，但可能支撑不住体重 |
| **damping (Kd)** | 运动更平滑，但响应迟钝 | 响应快，但可能振荡 |
| **action_scale** | 动作幅度大、力矩大，但不精细 | 动作精细、稳定，但范围受限 |

## 调参思路

### 第一步：确定 stiffness

按质量比例从已验证的配置推算：

```
A1 (12kg):  Kp=20  →  Kp/mass = 1.67
Go1 (12kg): Kp=30  →  Kp/mass = 2.50
```

XTDog (28kg) 合理范围：28 × (1.67~2.50) = **47~70**

当前 Kp=80 对应 Kp/mass=2.86，比 Go1 还高。对于跑酷需要较强的支撑力，**80 可以保留**，但如果发现关节抖动可以降到 60~70。

### 第二步：确定 damping

保持 Kd/Kp ≈ 0.025（与 A1、非 parkour XTDog 一致）：

```
Kp=80 → Kd = 80 × 0.025 = 2.0
Kp=70 → Kd = 70 × 0.025 = 1.75
```

当前 Kd=4.0（Kd/Kp=0.05）是过阻尼的，跑酷需要快速腿部动作，**应降到 2.0**。

### 第三步：确定 action_scale

根据 τ_max 反推：

```
action_scale = 0.15 → τ_max = 80 × 0.15 × 1.2 = 14.4 N·m (36% of 40)  偏保守
action_scale = 0.25 → τ_max = 80 × 0.25 × 1.2 = 24.0 N·m (60% of 40)  适中
action_scale = 0.30 → τ_max = 80 × 0.30 × 1.2 = 28.8 N·m (72% of 40)  激进
```

对于跑酷，0.15 太保守（跳跃力矩不足），**0.25 是合理的**。

## 推荐配置

```python
stiffness = {'joint': 80.0}   # 保持，28kg需要较强支撑
damping = {'joint': 2.0}      # 从4.0降低，Kd/Kp=0.025，保证快速响应
action_scale = 0.25            # 保持，跑酷需要足够的动作范围
```

关键变化就是 **damping 从 4.0 降到 2.0**，其他两个保持不变。

如果训练后观察到：
- **关节振荡/抖动** → damping 提到 2.5
- **跳跃高度不足** → action_scale 提到 0.3
- **关节过于僵硬** → stiffness 降到 60~70（同时 damping 按比例降到 1.5~1.75）
